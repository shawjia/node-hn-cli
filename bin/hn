#!/usr/bin/env node

var hn = require('hn.js');

hn.home(function(err, items){
  if (err) return console.error(err);

  var i;

  for (i in items) {
    console.log('#%s %s\n\t(%s)', items[i].id, items[i].title, items[i].url);
  }
});
